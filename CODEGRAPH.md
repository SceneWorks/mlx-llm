# mlx-llm

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-06-26 03:08 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

mlx-llm is a Rust repository that implements a local LLM inference server built on top of Apple's MLX framework. It exposes an OpenAI-compatible HTTP API (chat completions endpoint) served over a TCP-based HTTP server. The core architecture centers on a TextLlm provider trait/interface that abstracts LLM inference, with the server routing incoming chat completion requests to this provider. The repository also contains diagnostic and validation code for testing the correctness of scaled dot-product attention (SDPA) and matrix multiplication operations across different numeric data types (e.g., float16, bfloat16, float32), which suggests active development and numerical correctness verification of the MLX-based inference backend. There is a single project in the repository; all components—HTTP server, LLM provider interface, and attention/matmul test utilities—work together as one cohesive binary or library crate. The primary external dependency is the MLX framework (Apple's machine learning acceleration framework for Apple Silicon). No downstream dependents are identified within this repository.

## Projects

### mlx-llm

mlx-llm is a Rust project that implements a local LLM inference server using the MLX framework, exposing an OpenAI-compatible HTTP API for chat completions. It includes a TCP-based HTTP server that handles chat requests via a TextLlm provider interface, as well as example/diagnostic code for reproducing and validating scaled dot-product attention (SDPA) and matrix multiplication correctness across different data types.

