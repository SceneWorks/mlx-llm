# mlx-llm

> **Confidence**: Medium
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-06-24 02:02 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

mlx-llm is a Rust project that implements a local LLM inference server built on top of Apple's MLX array computation framework. It exposes an OpenAI-compatible HTTP API for chat completions over TCP, enabling local language model inference on Apple Silicon hardware. The repository also includes tooling to reproduce and validate the numerical accuracy of scaled dot-product attention (SDPA) across different data types (f32, f16, bf16) and attention configurations such as grouped-query attention (GQA) and causal masking. The project appears to be a single cohesive codebase with no cross-repository dependencies identified: the inference server and the SDPA validation tooling are both part of the same project, with the validation component likely serving as a correctness/debugging utility for the core attention mechanism used in LLM inference.

## Projects

### mlx-llm

mlx-llm is a Rust project that implements a local LLM inference server using the MLX framework (Apple's array computation library). It provides an HTTP server that exposes an OpenAI-compatible chat completion API over TCP, and includes tooling to reproduce and validate scaled dot-product attention (SDPA) numerical accuracy across different data types (f32, f16, bf16) and configurations such as grouped-query attention (GQA) and causal masking.

