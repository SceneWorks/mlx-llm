# mlx-llm

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-06-26 13:02 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

mlx-llm is a Rust project that implements a local LLM inference server built on top of Apple's MLX machine learning framework. It exposes an HTTP server with an OpenAI-compatible chat completion API, enabling clients to interact with locally-hosted language models using the same interface as OpenAI's API. The repository contains a single project with no cross-repo dependencies identified. Key technical components include: an HTTP server layer that translates OpenAI-compatible API requests into MLX inference calls, attention mechanism implementations (scaled dot-product attention and grouped-query attention), and numerical precision validation test/example code covering f32, f16, bf16 data types. The project depends on Apple's MLX framework (targeting Apple Silicon hardware where MLX is designed to run) and the Rust ecosystem. No downstream dependents were identified. The repository appears to be either a standalone tool or an early-stage library for running LLM inference locally on Apple hardware with a drop-in OpenAI API compatibility layer.

## Projects

### mlx-llm

mlx-llm is a Rust project that provides a local LLM inference server using the MLX framework (Apple's array framework for machine learning). It exposes an HTTP server that implements an OpenAI-compatible chat completion API, and includes example/test code for validating scaled dot-product attention (SDPA) numerical precision across different data types (f32, f16, bf16) and grouped-query attention (GQA) configurations.

