# mlx-llm

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-06-23 14:02 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

mlx-llm is a Rust library and application suite for running large language model inference on Apple Silicon using the MLX array framework. The repository has three primary components that work together: (1) a core inference engine implementing LLM forward passes and text generation logic using MLX bindings; (2) a numerical accuracy test suite that validates scaled dot-product attention (SDPA) operations across multiple floating-point data types (e.g., f16, bf16, f32), ensuring correctness of the MLX-based attention implementation; and (3) a lightweight HTTP server that exposes an OpenAI-compatible chat completions API endpoint, allowing clients to interact with locally-running LLMs via standard REST calls. The components are tightly coupled: the HTTP server delegates to the core inference engine, and the test suite exercises the same low-level attention kernels used during inference. The project depends on Apple's MLX framework (via Rust FFI or bindings) and targets Apple Silicon hardware exclusively. No downstream consumers are identified in the repository itself, but the OpenAI-compatible API surface suggests it is designed to serve as a drop-in local inference backend for tools that speak the OpenAI chat completions protocol.

## Projects

### mlx-llm

mlx-llm is a Rust project that provides large language model (LLM) inference using the MLX framework (Apple's array framework for Apple Silicon). It includes a text generation example, a numerical accuracy reproduction/test suite for scaled dot-product attention (SDPA) operations across different data types, and a lightweight HTTP server exposing a chat completion API compatible with OpenAI-style endpoints.

