# mlx-llm

> **Confidence**: Medium
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-06-24 01:02 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

mlx-llm is a Rust repository focused on LLM inference using the MLX framework (Apple's machine learning framework). It has two primary technical responsibilities: (1) a correctness/accuracy testing harness for scaled dot-product attention (SDPA) kernels across different numeric dtypes, used to validate attention computation results; and (2) a minimal TCP-based HTTP server that exposes an OpenAI-compatible chat completion API endpoint, delegating inference work to an internal TextLlm provider. The two components share the same codebase — the attention testing infrastructure validates the low-level numerical correctness of the MLX-backed kernels, while the server layer provides a higher-level API interface for consuming LLM inference. External dependencies center on the MLX framework for tensor operations and attention computation. No downstream dependents were identified in the analysis. The project appears to be a self-contained inference runtime and evaluation tool rather than a reusable library.

## Projects

### mlx-llm

mlx-llm is a Rust project that implements LLM (Large Language Model) inference using the MLX framework, providing both a reproduction/testing harness for scaled dot-product attention (SDPA) correctness across dtypes and a minimal HTTP server exposing an OpenAI-compatible chat completion API. The project covers numerical accuracy testing of attention kernels and a TCP-based inference server that delegates to a TextLlm provider.

