# mlx-llm

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-06-26 16:02 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

mlx-llm is a Rust repository that implements a local LLM inference server targeting Apple Silicon hardware via the MLX framework. It exposes an OpenAI-compatible HTTP API for chat completions, enabling clients that speak the OpenAI protocol to route requests to locally-running models without modification. The repository also contains numerical validation utilities (sdpa_f32_repro) that test the correctness of scaled dot-product attention (SDPA) and matrix multiplication operations across multiple data types and configurations — likely used to verify MLX kernel behavior or debug precision issues during development. The two components share the same codebase: the inference server is the primary deliverable, while the validation examples serve as a testing and debugging aid for the underlying numerical operations the server depends on. External dependencies center on the MLX framework for accelerated ML computation on Apple Silicon, and the HTTP server layer for protocol compatibility. Nothing in the repository appears to depend on this project externally.

## Projects

### mlx-llm

mlx-llm is a Rust project that provides a local LLM inference server built on top of the MLX framework (Apple Silicon ML). It includes an HTTP server component that exposes an OpenAI-compatible chat completion API over TCP, and a set of numerical validation examples (sdpa_f32_repro) for testing scaled dot-product attention (SDPA) and matrix multiplication correctness across different data types and configurations.

