# mlx-llm

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-06-22 15:02 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

mlx-llm is a Rust library/application for large language model (LLM) inference on Apple hardware using the MLX array framework. It implements LLaMA-style transformer architectures with components including multi-head attention (with grouped-query attention support), rotary position embeddings (RoPE) with multiple configuration variants, feed-forward networks, and RMS normalization. The project supports loading models in GGUF format (with quantized weight conversion), manages KV-cache via a block pooling system for efficient memory reuse, and provides both batched and continuous/streaming token generation. Configuration is driven by model metadata (context length, hidden dimensions, attention heads, RoPE parameters, etc.), allowing it to support a range of LLaMA-compatible model variants. As a single-project repository, all components are self-contained within one Rust crate. It depends on the MLX framework for array operations and GPU acceleration on Apple Silicon, and on GGUF file parsing for model weight ingestion. No downstream dependents are identified from the repository structure alone.

## Projects

### mlx-llm

mlx-llm is a Rust library/application for running large language model inference using the MLX framework (Apple's array framework). It implements LLaMA-style transformer models with support for GGUF model format conversion, batched and continuous (streaming) token generation, KV-cache management with block pooling, and configurable model architectures including various attention and RoPE configurations.

