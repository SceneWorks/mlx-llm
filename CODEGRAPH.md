# mlx-llm

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-06-23 21:01 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

mlx-llm is a Rust repository that implements local LLM inference infrastructure built on the MLX array framework. It provides two main technical components: (1) an HTTP server that exposes a chat completion API with OpenAI-compatible streaming SSE responses, backed by a TextLlm trait abstraction that allows different LLM backends to be plugged in; and (2) numerical validation utilities and example code for testing the accuracy of core ML operations—specifically scaled dot-product attention (SDPA) and matrix multiplication—across multiple floating-point data types (f32, f16, bf16). The project appears to be a self-contained library/server with no detected cross-repository dependencies. It depends on the MLX array framework for tensor computation and uses Rust's async ecosystem for the HTTP server layer. There are no known dependents. The repository is oriented toward local, on-device LLM inference with a focus on numerical correctness and API compatibility.

## Projects

### mlx-llm

mlx-llm is a Rust project that implements a local LLM inference server and associated numerical validation utilities. It provides an HTTP server exposing a chat completion API (compatible with OpenAI-style streaming SSE responses) backed by a TextLlm trait, and includes example code for reproducing and validating scaled dot-product attention (SDPA) and matrix multiplication numerical accuracy across different data types (f32, f16, bf16) using the MLX array framework.

