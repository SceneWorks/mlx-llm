# mlx-llm

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-06-22 17:02 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

mlx-llm is a Rust-based HTTP server that implements an OpenAI-compatible chat completions API as a local inference gateway. It exposes endpoints that accept standard OpenAI-format chat completion requests, routes them to a local large language model backend via a `TextLlm` trait abstraction, and returns responses in the OpenAI wire format — including support for streaming token-by-token output via Server-Sent Events (SSE). The name suggests it is designed to work with Apple's MLX framework (a machine learning framework for Apple Silicon), likely serving as a bridge between MLX-powered local LLMs and tooling that expects the OpenAI API interface. The repository appears to be a single-project codebase with no observed cross-repository dependencies; it is intended to be depended upon by clients (applications, tools, or users) that target the OpenAI chat completions API but want to run inference locally.

## Projects

### mlx-llm

mlx-llm is a Rust-based HTTP server that exposes an OpenAI-compatible chat completions API, routing requests to a local large language model provider (`TextLlm` trait). It handles HTTP request parsing, streaming Server-Sent Events (SSE) for token-by-token output, and JSON response formatting that mirrors the OpenAI chat completions wire format.

