# mlx-llm

> **Confidence**: Medium
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-06-23 13:02 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

mlx-llm is a Rust project that implements large language model (LLM) inference using Apple's MLX array framework. The repository provides: (1) a library implementing core LLM inference primitives including scaled dot-product attention (SDPA) and text generation logic, (2) an HTTP server that exposes LLM completion endpoints for serving model inference over a network, and (3) example/validation programs that test text generation correctness and verify SDPA behavior across different data types and configurations. The project targets Apple Silicon hardware via the MLX framework, suggesting it is oriented toward on-device or local inference on Apple platforms. All components are part of a single Rust workspace; the library provides shared inference logic consumed by both the HTTP server binary and the example programs. There are no identified cross-repository dependencies, meaning this project is self-contained. External dependencies include the MLX Rust bindings and likely an HTTP server framework, though the specific crate names are not listed in the provided evidence.

## Projects

### mlx-llm

mlx-llm is a Rust project that implements large language model inference using the MLX framework (Apple's array framework for machine learning). It provides both an HTTP server for serving LLM completions and example programs for running text generation and validating scaled dot-product attention (SDPA) correctness across different data types and configurations.

